import numpy as np 
import numba as nb

def genetic_algorithm(mu: int, p_i: float, p_s: float, time_budget: float):
   """Runs a genetic algorithm with a bidirectional greedy crossover, and 
      a mutation operator based on the SDR tensor."""
   pass 

